git config --global user.name "user name"
git config --global user.email "email git"

git init
git add .
git remote add origin "https://github.com/nimshidmohammed/demo_repo.git"
git commit -m "msg"
git push -u origin master
